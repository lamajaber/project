<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SignIn_Up.NotebookTutorialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".NoteBook.HomePageActivity" />
        <activity android:name=".Add_View.ViewMyNote" />
        <activity android:name=".Add_View.ShowAllNotes" />
        <activity android:name=".NoteBook.AllNoteBook" />
        <activity android:name=".NoteBook.AddNoteBookActivity" />
        <activity android:name=".Add_View.AllNotes" />
        <activity android:name=".SignIn_Up.SplashActivity" />
        <activity android:name=".SignIn_Up.SignUpActivity" />
        <activity android:name=".SignIn_Up.SignInActivity" />

        <activity android:name=".Add_View.AddNoteActivity" />
        <activity android:name=".NoteBook.HomePageActivity" />
        <activity android:name=".SignIn_Up.ForgotPassActivity" />

    </application>

</manifest>